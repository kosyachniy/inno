version: "3.9"
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRESQL_PASS}
      PGDATA: /data/db
      POSTGRES_DB: main
    volumes:
      - ./data:/data/db
    ports:
      - 5432:5432

  api:
    image: node
    env_file: .env
    volumes:
      - ./api:/app
    working_dir: /app
    ports:
      - ${API_PORT}:3000
    depends_on:
      - db
    entrypoint: bash -c "npm i && node server.js"

  app:
    image: node
    volumes:
      - ./app:/app
    working_dir: /app
    ports:
      - ${APP_PORT}:3000
    entrypoint: bash -c "npm i && npm run build && npm start"
